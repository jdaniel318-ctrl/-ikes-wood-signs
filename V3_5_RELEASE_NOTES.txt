BLACK FLAG ENGINE v3.5 — PLATFORM BRAND SYSTEM

Branding-only integration pass.

Implemented:
- Black Flag identity expanded beyond the Engine access screen into Engine-owned sections.
- Structural Status, Waters Ahead, Project Command, Configure/Commissioning surfaces receive restrained platform cues.
- Existing Project Control Center platform cue remains.
- Floating ENGINE return controls inside project shells are replaced by the approved Black Flag mark at approximately the same footprint.
- Existing button IDs/classes/data attributes and routing behavior are preserved.
- Current and future projects inherit the shared return-control styling where the shared ENGINE control is used.
- Project-specific branding remains dominant inside each project; Black Flag identifies the surrounding platform only.
- The approved brand rule remains intact: the flag itself stays BLACK and contrast is supplied by its field/background.

No project/order/owner/kiosk/commissioning logic changed.

Validation:
- JavaScript syntax PASS
- Duplicate IDs PASS
- CSS structure PASS
- Black Flag asset presence PASS
- Shared project return logo PASS
