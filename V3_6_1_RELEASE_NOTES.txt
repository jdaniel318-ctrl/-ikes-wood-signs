BLACK FLAG ENGINE v3.6.1 — HARBOR REPAIR

PURPOSE
Restore the shared Black Flag return control in project shells before Dark Sky leaves harbor.

FIXES
- Corrected an older project-isolation CSS rule that unintentionally hid the shared Black Flag return control because its class name contains "black-flag".
- Added a narrowly scoped project-mode override for #returnToEngineBtn only; no other Black Flag platform elements are exposed inside project customer shells.
- Rebuilt black_flag_platform_icon.png from the approved Black Flag emblem crop, removing stray option-label/wordmark material from the prior malformed icon asset.
- The return mark remains fixed in the lower-right and intentionally subdued (78% opacity) until hover/focus so project branding stays dominant.
- Existing click routing is preserved: selecting the mark locks Engine authorization and opens the Black Flag Engine PIN gate; CLOSE returns to the same project.
- Cache/version references bumped to v3.6.1 to reduce stale GitHub Pages/PWA asset behavior.

BOUNDARY
This is a surgical platform-chrome repair. No order, project, customer, owner, pricing, commissioning, ledger, or Captain logic was intentionally changed.
