BLACK FLAG ENGINE v3.6 — PLATFORM BRAND REBUILD

This pass corrects v3.4/v3.5's weak brand integration and the project return-control regression.

WHAT CHANGED
- Black Flag is now the visual identity of Engine-owned surfaces rather than a small decorative cue.
- Engine Room title now uses a Black Flag platform lockup: mark + BLACK FLAG + ENGINE ROOM + BUSINESS COMMAND PLATFORM.
- Major Engine sections carry restrained Black Flag instrumentation labels.
- Engine-owned panels share a consistent Black Flag border/accent system.
- A restrained persistent Black Flag signature is visible while inside the Engine.
- Generic Engine icon containers are visually replaced by the platform mark where shared classes are present.
- Project-specific branding remains dominant inside each project.

PROJECT RETURN CONTROL FIX
- The old floating ENGINE pill is represented by the approved Black Flag platform mark.
- It is explicitly fixed to the lower-right, visible, opaque, and above project content.
- Existing routing attributes/handlers are preserved.
- It is deliberately small (~46 px), matching the prior Engine control footprint.
- Current/future projects using the shared return control inherit this behavior.

BRAND RULE
- The flag itself remains BLACK.
- The logo uses a light neutral field where required for contrast.
- No rejected BF-monogram/cutout-flag treatment is used.

FUNCTIONALITY
- No project, order, owner, kiosk, commissioning, ledger, or Captain logic was intentionally changed.

VALIDATION
- JavaScript syntax PASS
- Duplicate static IDs PASS
- CSS structure PASS
- Platform icon asset PASS
- Project return button present PASS
- Project return visibility rules PASS
- Engine brand lockup PASS
- Project Control return handler preserved PASS
