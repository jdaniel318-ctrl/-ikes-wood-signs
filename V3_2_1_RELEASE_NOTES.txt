BLACK FLAG ENGINE v3.2.1 — OWNER + CUSTOMER DEVICE REFINEMENT

Second-pass refinement of v3.2.

Refined:
- Centralized deployment/device identity normalization so Captain and Owner paths cannot drift.
- Device state is derived consistently from deployment lifecycle: Authorized, Sea Trial, Active, Paused, Revoked.
- Owner pause/resume actions now create explicit deployment audit events.
- Owner and Engine device views clearly show project-only trust boundaries.
- Engine deployment manifests now show device ID, authorization state, project-only scope, and no-Engine-access status.
- Deployment fleet snapshots include authorization state and device ID.
- Revocation/retirement remains history-preserving.
- Existing project isolation, Sea Trial, commissioning, filtering, Owner Portal and Captain controls are preserved.

Production boundary unchanged:
Server-backed authentication, secure sessions, server-side authorization, recovery and revocation enforcement are still required before real external deployment.
